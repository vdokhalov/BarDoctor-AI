name: Repair BarDoctor source checksum
on:
  push:
    branches:
      - codex-canonical-taxonomy-v336-sync
permissions:
  contents: write
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Repair exact source files
        shell: bash
        run: |
          while IFS=$'\t' read -r source_dir target; do
            mkdir -p "$(dirname "$target")"
            cat ".github/bardoctor-repair/$source_dir/"*.part | base64 --decode > "$target"
          done < .github/bardoctor-repair/manifest.tsv
          rm -rf .github/bardoctor-repair
          rm -f .github/workflows/bardoctor-source-repair.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet || git commit -m "chore: repair exact BarDoctor source checksum"
          git push origin HEAD:codex-canonical-taxonomy-v336-sync
